{
  "name": "haze",
  "private": true,
  "version": "2.0.0",
  "description": "A first-person browser survival game with a verified global leaderboard.",
  "type": "module",
  "scripts": {
    "build": "node --check src/worker.js && node build.mjs",
    "check": "npm run build && npm test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "0.128.0"
  },
  "devDependencies": {
    "vitest": "4.0.18",
    "wrangler": "4.120.0"
  }
}
