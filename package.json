{
  "name": "haze",
  "private": true,
  "version": "2.0.0",
  "description": "A first-person browser survival game with a verified global leaderboard.",
  "type": "module",
  "scripts": {
    "build": "node --check src/entry.js && node --check src/worker.js && node --check build.mjs && node --check optimize.mjs && node build.mjs && node optimize.mjs",
    "check": "npm run build && npm test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "0.128.0"
  },
  "devDependencies": {
    "vitest": "4.1.10",
    "wrangler": "4.120.0"
  }
}
